---
phase: 02-release-boundary-validation
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - .planning/phases/02-release-boundary-validation/02-VALIDATION.md
  - .planning/phases/02-release-boundary-validation/02-VERIFICATION.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements: [REL-01, VAL-01]
user_setup: []
must_haves:
  truths:
    - "REL-01: Phase 2 records a clean, tested release-candidate commit SHA whose AppContent.vue lacks all four debounce symbols; it does not create the formal v1.0 tag."
    - "REL-01: The exact pre-isolation AppContent.vue binary diff is restored, remains unstaged, and the immutable stash object remains recoverable."
    - "VAL-01: Validation, verification, and audit evidence identify the candidate SHA and every preservation check."
  artifacts:
    - { path: ".planning/phases/02-release-boundary-validation/02-VALIDATION.md", provides: "Candidate-boundary and restoration evidence", contains: "REL-01" }
    - { path: ".planning/phases/02-release-boundary-validation/02-VERIFICATION.md", provides: "Final phase verification", contains: "status: passed" }
    - { path: ".planning/v1.0-MILESTONE-AUDIT.md", provides: "Audit result against candidate commit", contains: "candidate" }
  key_links:
    - { from: "recorded candidate SHA", to: "src/AppContent.vue", via: "git show plus four symbol-negative checks", pattern: "saveConfigDebounceDelay|saveConfigTimer|scheduleSaveConfig|onBeforeUnmount" }
    - { from: "pre-isolation binary diff", to: "stash and restored diff", via: "exact SHA-256 equality", pattern: "SHA-256" }
---

<objective>
Prove a clean v1.0 release-candidate commit while preserving the deferred debounce byte-for-byte.

Purpose: Close REL-01 without sacrificing user-owned work. Formal annotated `v1.0` tag creation remains exclusively with `$gsd-complete-milestone` after final audit closure.
Output: Candidate SHA, restored debounce patch, finalized validation/verification, and updated milestone audit.
</objective>

<execution_context>
@C:/Users/retur/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/retur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/02-release-boundary-validation/02-RESEARCH.md
@.planning/phases/02-release-boundary-validation/02-PATTERNS.md
@.planning/phases/02-release-boundary-validation/02-VALIDATION.md
@.planning/phases/02-release-boundary-validation/02-02-SUMMARY.md

<interfaces>
The protected source is exactly `src/AppContent.vue`; Phase 2 must never stage or commit it.
The preserved patch contains four independently checked symbols: `onBeforeUnmount`, `saveConfigDebounceDelay`, `saveConfigTimer`, and `scheduleSaveConfig`.
Phase 2 records a full candidate commit SHA. It must not invoke `git tag` or claim that `v1.0` already targets the candidate.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> stash object | User-owned content is temporarily isolated into Git object storage. |
| committed HEAD -> candidate evidence | A clean tested commit is recorded for milestone completion. |
| stash object -> working tree | Deferred work is restored without mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | deferred patch | mitigate | Compare pre-isolation, stash, and restored binary diff SHA-256 values exactly. |
| T-02-08 | Repudiation | candidate identity | mitigate | Record full HEAD SHA, clean status, commands, and candidate-content negative checks. |
| T-02-09 | Elevation of privilege | formal release tag | mitigate | Do not create/move/push tags; delegate formal tag to `$gsd-complete-milestone`. |
| T-02-10 | Denial of service | stash drift/conflict | mitigate | Persist immutable object ID, use apply not pop, retain object and stop on any failure/conflict. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fingerprint and isolate only the deferred AppContent patch</name>
  <files>.planning/phases/02-release-boundary-validation/02-VALIDATION.md</files>
  <read_first>src/AppContent.vue, .planning/phases/02-release-boundary-validation/02-VALIDATION.md</read_first>
  <action>
Record status, cached paths, `git diff --check -- src/AppContent.vue`, and exact bytes from `git diff --binary -- src/AppContent.vue`. Compute and persist their SHA-256. Require the file modified but unstaged and check each of the four symbols independently.

Run the uniquely named path-scoped stash command from the established pattern. Immediately persist its immutable object ID (never rely on a later `stash@{0}`), require `git cat-file -t <oid>` equals `commit`, and require the non-empty output of `git stash show --name-only <oid>` equals exactly `src/AppContent.vue`. Hash the exact bytes of `git stash show --binary -p <oid> -- src/AppContent.vue` and require equality with the pre-isolation SHA-256. Require clean worktree/index. On any failure, retain the stash and stop; never reset, restore, checkout, clean, pop, or broadly stash.
  </action>
  <verify>
    <automated>git cat-file -e "$env:PHASE02_STASH_OID^{commit}"; if (@(git stash show --name-only $env:PHASE02_STASH_OID | Where-Object { $_ }) -ne 'src/AppContent.vue') { exit 1 }; git diff --quiet -- src/AppContent.vue; git diff --quiet --cached -- src/AppContent.vue</automated>
  </verify>
  <acceptance_criteria>
    - Immutable stash OID, commit type, exact path inventory, and matching binary SHA-256 are recorded.
    - Only `src/AppContent.vue` was isolated and the candidate tree/index are clean.
  </acceptance_criteria>
  <done>The deferred patch is isolated with object, scope, and byte-integrity proof.</done>
</task>

<task type="auto">
  <name>Task 2: Validate and record the clean release-candidate SHA</name>
  <files>.planning/phases/02-release-boundary-validation/02-VALIDATION.md</files>
  <read_first>.planning/phases/02-release-boundary-validation/02-VALIDATION.md</read_first>
  <action>
With the patch absent and tree clean, run `pnpm test`, `pnpm build`, and `cargo check --manifest-path src-tauri/Cargo.toml`. Record full `git rev-parse HEAD` as the candidate SHA and the clean status. Use `git show <candidate>:src/AppContent.vue` to require each of the four symbols absent, and prove the candidate commit did not modify this file with `git diff --name-only <candidate>^..<candidate> -- src/AppContent.vue`.

Do not run `git tag`, inspect a tag as acceptance evidence, or assert that `v1.0` targets this SHA. Record that formal annotated tagging is pending `$gsd-complete-milestone`.
  </action>
  <verify>
    <automated>$candidate = git rev-parse HEAD; if (git status --porcelain) { exit 1 }; foreach ($symbol in @('saveConfigDebounceDelay','saveConfigTimer','scheduleSaveConfig','onBeforeUnmount')) { if (git show "${candidate}:src/AppContent.vue" | Select-String -SimpleMatch $symbol) { exit 1 } }</automated>
  </verify>
  <acceptance_criteria>
    - All automated gates pass from the clean candidate.
    - Full candidate SHA and four independent negative checks are recorded.
    - Phase 2 creates, moves, pushes, or asserts no tag.
  </acceptance_criteria>
  <done>The tested candidate SHA is auditable and ready for milestone-completion tagging.</done>
</task>

<task type="auto">
  <name>Task 3: Restore deferred work, finalize evidence, and re-audit readiness</name>
  <files>.planning/phases/02-release-boundary-validation/02-VALIDATION.md, .planning/phases/02-release-boundary-validation/02-VERIFICATION.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>.planning/phases/02-release-boundary-validation/02-VALIDATION.md, .planning/phases/02-release-boundary-validation/02-VERIFICATION.md, .planning/v1.0-MILESTONE-AUDIT.md</read_first>
  <action>
Apply the immutable stash OID, never pop. If apply returns non-zero or reports conflict, retain the stash, preserve evidence, and stop without finalizing or overwriting. Hash the restored `git diff --binary -- src/AppContent.vue` exact bytes and require exact pre-isolation equality. Require each of the four symbols individually present, require `git diff --cached --name-only -- src/AppContent.vue` empty, and require `git cat-file -e <oid>^{commit}` still succeeds.

Finalize `02-VALIDATION.md` and `02-VERIFICATION.md` with candidate SHA, clean-boundary checks, stash OID, all three SHA-256 values, restoration checks, and REL-01/UI-01/VAL-01 evidence. Re-run the milestone audit against the recorded candidate commit rather than the restored dirty tree. State that restored debounce work is outside the candidate and formal tag creation remains pending `$gsd-complete-milestone`. Stage only the three evidence files and prove `src/AppContent.vue` is absent from the index.
  </action>
  <verify>
    <automated>if (git diff --cached --name-only | Select-String -SimpleMatch 'src/AppContent.vue') { exit 1 }; foreach ($symbol in @('saveConfigDebounceDelay','saveConfigTimer','scheduleSaveConfig','onBeforeUnmount')) { if (-not (git diff -- src/AppContent.vue | Select-String -SimpleMatch $symbol)) { exit 1 } }; git cat-file -e "$env:PHASE02_STASH_OID^{commit}"; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - Restored binary diff SHA-256 equals the pre-isolation value exactly.
    - All four symbols exist, AppContent is unstaged, and the stash object remains resolvable.
    - Validation, verification, and audit target the candidate SHA and reserve tagging for milestone completion.
  </acceptance_criteria>
  <done>REL-01 closes without losing user work or preempting the formal milestone tag.</done>
</task>

</tasks>

<verification>
- [ ] Candidate SHA identifies the clean commit validated by test/build/cargo.
- [ ] Candidate AppContent lacks all four reserved symbols.
- [ ] Pre-isolation, stash, and restored binary diff SHA-256 values are identical.
- [ ] Four symbols are present after restore, AppContent is unstaged, and stash OID remains resolvable.
- [ ] No Phase 2 command creates or asserts a v1.0 tag.
</verification>

<success_criteria>
- A clean tested candidate SHA is ready for `$gsd-complete-milestone`.
- Deferred debounce work is excluded from the candidate and all Phase 2 commits while remaining restored and recoverable.
- REL-01, UI-01, and VAL-01 have complete evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-release-boundary-validation/02-03-SUMMARY.md`
</output>
