---
status: complete
date: 2026-05-04
---

# Summary

Added persisted E-Hentai favorites settings and startup update checking:

- Config now stores cookie, favorites URL, target folder, optional limit, auto-check flag, and known gallery ids.
- Search pane controls now edit the saved config directly.
- Startup flow checks E-Hentai favorites when enabled and queues only new gallery ids.
- The import command now returns found ids and skipped-known counts.

## Verification

- `cargo check` passed with the existing `LoginResp` dead-code warning.
- `pnpm.cmd build` passed after running outside the sandbox so Vite/esbuild could spawn its child process.
- Vite reported the existing non-blocking large chunk warning.

## Security Note

The cookie is persisted in the app config file for convenience. It is not encrypted in this quick task.
