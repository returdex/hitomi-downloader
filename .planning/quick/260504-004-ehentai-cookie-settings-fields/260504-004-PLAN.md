# Quick Task 260504-004: Move E-Hentai Cookie Fields Into Settings

## Description

Record E-Hentai login cookies in Settings using `ipb_member_id`, `ipb_pass_hash`, and `igneous`.

## Tasks

1. Config and helpers
   - Add separate config fields for the three cookie values.
   - Add shared helpers to validate and compose the Cookie header.

2. Settings UI
   - Add E-Hentai Cookie fields to Settings.
   - Use password inputs for sensitive hash values.

3. Search and startup integration
   - Remove full-cookie input from Search pane.
   - Use the saved Settings fields for manual import and startup auto-check.

4. Verification
   - Run frontend build and Rust checks.
