---
phase: 02-release-boundary-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - .planning/phases/02-release-boundary-validation/02-VALIDATION.md
  - .planning/phases/02-release-boundary-validation/02-VERIFICATION.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - REL-01
  - VAL-01
user_setup: []
must_haves:
  truths:
    - "REL-01: The local annotated v1.0 tag targets a committed boundary whose AppContent.vue lacks every reserved debounce symbol."
    - "REL-01: The exact pre-isolation AppContent.vue patch is restored after tagging, remains unstaged, and is not included in any Phase 2 commit."
    - "VAL-01: Validation and verification record the tag object, peeled commit, clean-boundary observation, negative checks, restoration fingerprint, and re-audit result."
  artifacts:
    - { path: ".planning/phases/02-release-boundary-validation/02-VALIDATION.md", provides: "Final release-boundary command evidence", contains: "REL-01" }
    - { path: ".planning/phases/02-release-boundary-validation/02-VERIFICATION.md", provides: "Passed phase verification after release closure", contains: "status: passed" }
    - { path: ".planning/v1.0-MILESTONE-AUDIT.md", provides: "Re-audit readiness/result against the tagged boundary", contains: "REL-01" }
  key_links:
    - { from: "v1.0 annotated tag", to: "src/AppContent.vue", via: "git show at tagged commit plus symbol-negative scan", pattern: "saveConfigDebounceDelay|saveConfigTimer|scheduleSaveConfig|onBeforeUnmount" }
    - { from: "pre-isolation patch fingerprint", to: "restored working-tree patch", via: "SHA-256 equality and git diff review", pattern: "SHA-256" }
    - { from: ".planning/v1.0-MILESTONE-AUDIT.md", to: "v1.0", via: "audit uses tag commit rather than restored dirty working tree as release scope", pattern: "v1\\.0" }
---

<objective>
Create and prove the clean v1.0 release boundary while preserving the deferred debounce byte-for-byte for the next milestone.

Purpose: Close REL-01 without sacrificing user-owned work or allowing a dirty working tree to contaminate release evidence.
Output: Verified local annotated tag, restored debounce patch, finalized validation/verification, and re-audit evidence.
</objective>

<execution_context>
@C:/Users/retur/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/retur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/02-release-boundary-validation/02-RESEARCH.md
@.planning/phases/02-release-boundary-validation/02-PATTERNS.md
@.planning/phases/02-release-boundary-validation/02-VALIDATION.md
@.planning/phases/02-release-boundary-validation/02-VERIFICATION.md
@.planning/phases/02-release-boundary-validation/02-02-SUMMARY.md

<interfaces>
The protected source is exactly `src/AppContent.vue`; it must never be staged or committed by Phase 2.
The expected deferred patch contains `onBeforeUnmount`, `saveConfigDebounceDelay`, `saveConfigTimer`, and `scheduleSaveConfig`.
Release acceptance is a verified local annotated `v1.0` tag. Do not push the tag, create a hosted release, or package from the restored dirty tree without separate user authorization.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> stash object | User-owned uncommitted content is temporarily isolated into Git object storage. |
| committed HEAD -> release tag | The locally audited commit becomes the immutable v1.0 reference. |
| stash object -> restored working tree | Deferred work returns to the next-milestone workspace after tagging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | deferred `AppContent.vue` patch | mitigate | Capture pre/post SHA-256 fingerprints, inspect the captured stash object, use `apply` rather than `pop`, and stop on mismatch/conflict. |
| T-02-08 | Repudiation | annotated `v1.0` tag | mitigate | Record tag object ID, peeled commit ID, clean status observation, and tag-content negative checks. |
| T-02-09 | Elevation of privilege | remote release publication | accept | No remote push or hosted release is authorized or performed; acceptance is local tag evidence only. |
| T-02-10 | Denial of service | stash reference drift | mitigate | Capture the stash commit object immediately and address it by immutable object ID, not a later `stash@{0}` ordinal. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fingerprint and isolate only the deferred AppContent patch</name>
  <files>.planning/phases/02-release-boundary-validation/02-VALIDATION.md</files>
  <read_first>src/AppContent.vue, .planning/phases/02-release-boundary-validation/02-PATTERNS.md, .planning/phases/02-release-boundary-validation/02-VALIDATION.md</read_first>
  <action>
From the repository root, record `git status --short`, `git diff --check -- src/AppContent.vue`, `git diff --cached --name-only`, and the full `git diff --binary -- src/AppContent.vue`. Compute a SHA-256 fingerprint over the exact binary diff bytes and record it in validation evidence. Require that `src/AppContent.vue` is modified but not staged and that the expected four debounce symbols occur in the working patch.

Create a uniquely named path-scoped stash with `git stash push -m "phase-02-preserve-appcontent-debounce" -- src/AppContent.vue`. Immediately capture the resulting stash commit object ID (do not retain only `stash@{0}`), inspect `git stash show -p <object-id> -- src/AppContent.vue`, and verify its diff fingerprint matches the pre-isolation value. Confirm `git status --short` is clean before proceeding. Never run reset, checkout, restore, clean, stash pop, or a broad stash; if any precondition or fingerprint fails, stop with the stash intact.
  </action>
  <verify>
    <automated>git diff --quiet -- src/AppContent.vue &amp;&amp; git diff --quiet --cached -- src/AppContent.vue</automated>
  </verify>
  <acceptance_criteria>
    - Pre-isolation status, patch, expected symbols, and SHA-256 fingerprint are recorded.
    - Exactly `src/AppContent.vue` is captured in the uniquely named stash.
    - The immutable stash object ID and matching stash-patch fingerprint are recorded.
    - The release candidate worktree and index are clean.
  </acceptance_criteria>
  <done>The deferred work is recoverably isolated with two independent integrity checks.</done>
</task>

<task type="auto">
  <name>Task 2: Validate and tag the committed v1.0 boundary</name>
  <files>.planning/phases/02-release-boundary-validation/02-VALIDATION.md</files>
  <read_first>.planning/phases/02-release-boundary-validation/02-VALIDATION.md, .planning/phases/02-release-boundary-validation/02-VERIFICATION.md</read_first>
  <action>
With the deferred patch absent and the tree clean, rerun `pnpm test`, `pnpm build`, and `cargo check --manifest-path src-tauri/Cargo.toml`. Confirm no existing `v1.0` tag is present; if it exists, stop and report its object/target rather than moving or deleting it. Create `git tag -a v1.0 -m "v1.0"` at the current committed HEAD.

Record `git rev-parse v1.0`, `git rev-parse v1.0^{}`, and `git rev-parse HEAD`, proving the peeled tag target equals HEAD. Inspect `git show v1.0:src/AppContent.vue` and perform a negative check for `saveConfigDebounceDelay|saveConfigTimer|scheduleSaveConfig|onBeforeUnmount`; all must be absent. Also prove `git diff --name-only v1.0^..v1.0 -- src/AppContent.vue` is empty, so the release commit did not introduce the deferred file. Do not push the tag or build/package from any later restored dirty tree.
  </action>
  <verify>
    <automated>git cat-file -t v1.0 | Select-String -SimpleMatch tag; if ((git rev-parse 'v1.0^{}') -ne (git rev-parse HEAD)) { exit 1 }; if (git show 'v1.0:src/AppContent.vue' | Select-String -Pattern 'saveConfigDebounceDelay|saveConfigTimer|scheduleSaveConfig|onBeforeUnmount') { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
    - All automated gates pass from the clean release candidate.
    - `v1.0` is an annotated tag whose peeled target is the validated HEAD.
    - Tagged `AppContent.vue` lacks every reserved debounce symbol.
    - No tag is overwritten, moved, pushed, or remotely published.
  </acceptance_criteria>
  <done>An auditable local v1.0 release boundary excludes the deferred capability.</done>
</task>

<task type="auto">
  <name>Task 3: Restore deferred work, finalize evidence, and re-audit readiness</name>
  <files>.planning/phases/02-release-boundary-validation/02-VALIDATION.md, .planning/phases/02-release-boundary-validation/02-VERIFICATION.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>.planning/phases/02-release-boundary-validation/02-VALIDATION.md, .planning/phases/02-release-boundary-validation/02-VERIFICATION.md, .planning/v1.0-MILESTONE-AUDIT.md</read_first>
  <action>
Apply the captured immutable stash object with `git stash apply <object-id>`; never use `pop`. If Git reports a conflict, stop, preserve the stash and conflict evidence, and do not overwrite either side. Recompute `git diff --binary -- src/AppContent.vue` SHA-256 and require exact equality with the pre-isolation fingerprint. Confirm the file remains unstaged and the four debounce symbols are present. Keep the stash until the user persists the work in the next milestone; do not drop it during Phase 2.

Append the actual tag object, peeled commit, clean-boundary observation, negative-symbol result, stash object, restoration fingerprint, and passing commands to `02-VALIDATION.md`; clear all REL-01 pending rows only now. Finalize `02-VERIFICATION.md` to `status: passed` with REL-01/UI-01/VAL-01 evidence.

Re-run the milestone audit workflow against the `v1.0` tag/peeled commit as the release boundary, not against the restored dirty working tree, and update `.planning/v1.0-MILESTONE-AUDIT.md` with the result or explicit remaining gaps. The audit must note that the restored `AppContent.vue` patch is preserved next-milestone work outside the tag. Stage/commit only the three named evidence files; assert `git diff --cached --name-only` does not contain `src/AppContent.vue`.
  </action>
  <verify>
    <automated>if (git diff --cached --name-only | Select-String -SimpleMatch 'src/AppContent.vue') { exit 1 }; if (-not (git diff -- src/AppContent.vue | Select-String -Pattern 'saveConfigDebounceDelay|saveConfigTimer|scheduleSaveConfig|onBeforeUnmount')) { exit 1 }; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - The restored patch fingerprint exactly matches the pre-isolation fingerprint and remains unstaged.
    - The preservation stash remains available and is not dropped.
    - Validation and verification contain reproducible final REL-01 evidence.
    - The milestone audit evaluates the tag boundary and records readiness or explicit residual gaps.
    - No Phase 2 commit or the v1.0 tag contains the debounce implementation.
  </acceptance_criteria>
  <done>REL-01 is closed, user work is preserved for the next milestone, and re-audit evidence is ready.</done>
</task>

</tasks>

<verification>
- [ ] The annotated `v1.0` tag peels to the clean commit validated by test/build/cargo.
- [ ] Tagged `src/AppContent.vue` lacks all four reserved debounce symbols.
- [ ] Restored `src/AppContent.vue` matches its pre-isolation binary-diff SHA-256 and is unstaged.
- [ ] The preservation stash still exists after apply.
- [ ] `02-VALIDATION.md` and `02-VERIFICATION.md` contain final evidence for REL-01, UI-01, and VAL-01.
- [ ] The re-audit targets the tag boundary and reports any remaining gap explicitly.
</verification>

<success_criteria>
- v1.0 has a locally auditable, immutable release boundary.
- Deferred debounce work is excluded from v1.0 and all Phase 2 commits while remaining recoverable and restored.
- Automated validation evidence is complete and re-audit-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/02-release-boundary-validation/02-03-SUMMARY.md`
</output>
