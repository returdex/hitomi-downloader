---
phase: 02-release-boundary-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .planning/phases/02-release-boundary-validation/02-VALIDATION.md
  - .planning/phases/02-release-boundary-validation/02-VERIFICATION.md
autonomous: true
requirements:
  - UI-01
  - VAL-01
user_setup: []
must_haves:
  truths:
    - "VAL-01: Focused tests, the full frontend suite, the production frontend build, and Rust checks all have recorded passing evidence."
    - "VAL-01: Nyquist evidence maps UI-01, VAL-01, and the pending release-boundary checks to exact automated commands and artifacts."
    - "UI-01: Phase verification independently confirms failure retention and success publication."
  artifacts:
    - { path: ".planning/phases/02-release-boundary-validation/02-VALIDATION.md", provides: "Complete Nyquist validation contract and execution evidence", contains: "nyquist_compliant: true" }
    - { path: ".planning/phases/02-release-boundary-validation/02-VERIFICATION.md", provides: "Goal-backward phase verification evidence", contains: "status: passed" }
  key_links:
    - { from: ".planning/phases/02-release-boundary-validation/02-VALIDATION.md", to: "src/panes/SearchPane.spec.ts", via: "requirement-to-test mapping and exact command", pattern: "SearchPane\\.spec\\.ts" }
    - { from: ".planning/phases/02-release-boundary-validation/02-VERIFICATION.md", to: ".planning/REQUIREMENTS.md", via: "UI-01 and VAL-01 evidence rows", pattern: "UI-01|VAL-01" }
---

<objective>
Complete automated validation and produce auditable Nyquist plus phase-verification evidence before creating the release tag.

Purpose: Make the v1.0 closure claims reproducible from commands and artifacts rather than prose-only assertions.
Output: Passing full validation gates, complete `02-VALIDATION.md`, and `02-VERIFICATION.md`.
</objective>

<execution_context>
@C:/Users/retur/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/retur/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/02-release-boundary-validation/02-RESEARCH.md
@.planning/phases/02-release-boundary-validation/02-01-SUMMARY.md
@.planning/phases/01-stabilization-cleanup/01-VERIFICATION.md
@src/panes/SearchPane.spec.ts

<interfaces>
Plan 01 provides the `pnpm test` one-shot gate and the focused `SearchPane.spec.ts`.
The final release-boundary object IDs and symbol-negative results are appended by Plan 03; this plan must define those REL-01 rows as explicit pending release gates, not falsely mark them verified.
`src/AppContent.vue` remains user-owned, unstaged next-milestone work and is never a file modified or committed by this plan.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command output -> validation artifact | Generated test/build/check output is summarized as release evidence. |
| dirty worktree -> phase commits | User-owned debounce work coexists with validation-document commits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Repudiation | `02-VALIDATION.md` | mitigate | Record exact commands, pass/fail status, measured test count/duration, and dated sign-off. |
| T-02-05 | Tampering | Git index | mitigate | Stage only the two named planning artifacts and assert the cached path list excludes `src/AppContent.vue`. |
| T-02-06 | Information disclosure | validation logs | mitigate | Record outcomes and known warnings without copying config, cookies, or filesystem secrets. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run the focused and full automated validation gates</name>
  <files>.planning/phases/02-release-boundary-validation/02-VALIDATION.md</files>
  <read_first>package.json, src/panes/SearchPane.spec.ts, .planning/phases/02-release-boundary-validation/02-RESEARCH.md</read_first>
  <action>
Run, from the repository root, `pnpm test -- src/panes/SearchPane.spec.ts`, `pnpm test`, `pnpm build`, and `cargo check --manifest-path src-tauri/Cargo.toml`. Capture exact outcomes, test count and measured duration, and distinguish the already-known Vite large-chunk and Rust `LoginResp` warnings from new failures.

Create `02-VALIDATION.md` using the researched schema: complete frontmatter, Test Infrastructure, Requirements-to-Test Map, Wave 0 checklist, Evidence, and Sign-off. Set `wave_0_complete: true` and `nyquist_compliant: true` only after every runnable gate passes. Mark UI-01 and VAL-01 green; define REL-01's path fingerprint, clean-boundary, annotated-tag, negative-symbol, and restoration checks as named pending gates for Plan 03. Do not invent tag evidence before the tag exists.
  </action>
  <verify>
    <automated>pnpm test &amp;&amp; pnpm build &amp;&amp; cargo check --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Focused and full frontend tests pass in non-watch mode.
    - Production frontend build and Rust check pass.
    - `02-VALIDATION.md` records exact evidence and has no pending UI-01/VAL-01 row.
    - REL-01 release operations remain explicit pending gates for Plan 03 rather than fabricated evidence.
  </acceptance_criteria>
  <done>Automated frontend, build, and Rust evidence is complete and Nyquist-compliant for the implemented behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Produce independent phase verification for implemented behavior</name>
  <files>.planning/phases/02-release-boundary-validation/02-VERIFICATION.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-release-boundary-validation/02-VALIDATION.md, .planning/phases/01-stabilization-cleanup/01-VERIFICATION.md</read_first>
  <action>
Create `02-VERIFICATION.md` following the Phase 1 verification structure: frontmatter, Result, Must-Haves Checked, Automated Checks, Human Verification, and Gaps. Cite exact tests/files for UI-01 and VAL-01. State that the phase implementation/validation portion passes but REL-01 final closure is gated on Plan 03's annotated tag and restoration evidence; do not claim the whole phase is passed until those gates are appended. After writing both artifacts, stage only their exact paths and confirm `git diff --cached --name-only` excludes `src/AppContent.vue`.
  </action>
  <verify>
    <automated>pnpm test -- src/panes/SearchPane.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every UI-01 and VAL-01 claim cites a reproducible command and concrete artifact.
    - Known warnings are recorded as pre-existing and non-blocking.
    - REL-01 remains visibly gated, enabling honest finalization in Plan 03.
    - The user debounce diff remains unstaged and unmodified.
  </acceptance_criteria>
  <done>Phase verification is ready for release-boundary evidence without overstating completion.</done>
</task>

</tasks>

<verification>
- [ ] Focused test, full test, frontend build, and Rust check pass.
- [ ] `02-VALIDATION.md` has complete infrastructure, mapping, Wave 0, evidence, and sign-off sections.
- [ ] `02-VERIFICATION.md` distinguishes completed automated behavior from pending REL-01 tag closure.
- [ ] Only explicit planning artifact paths are staged/committed.
</verification>

<success_criteria>
- VAL-01 has complete automated and Nyquist evidence.
- UI-01 is independently traceable from requirement to test to implementation.
- Release-boundary checks are executable and cannot be silently skipped.
</success_criteria>

<output>
After completion, create `.planning/phases/02-release-boundary-validation/02-02-SUMMARY.md`
</output>
